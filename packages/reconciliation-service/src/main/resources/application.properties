spring.application.name=reconciliation-service

# Only the health endpoint is exposed. It is the deploy verify gate defined in
# docs/specs/stack.md; nothing else is needed over HTTP for operations yet.
management.endpoints.web.exposure.include=health
management.endpoint.health.probes.enabled=true
